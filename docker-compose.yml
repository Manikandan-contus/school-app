version: '3.8'

services:
  web:
    build: .
    container_name: school-app-container
    ports:
      - "3000:8080"  # Maps port 3000 on AWS to Vite's 5173 inside the container
    restart: always
    environment:
      - HOST=0.0.0.0 # Force Vite to listen globally so AWS can reach it
